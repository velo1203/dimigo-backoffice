import styled from "styled-components";

export const StyledStay = styled.div`
    padding: 40px;
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-template-rows: repeat(2, auto);
    grid-gap: 50px;
`;
